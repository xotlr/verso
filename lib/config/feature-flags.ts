/**
 * Feature Flags
 *
 * Configuration for feature toggles.
 */

/**
 * Use Supabase Auth for authentication.
 * This is now the default and only supported auth system.
 */
export const USE_SUPABASE_AUTH = true

/**
 * Use Supabase database.
 * This is now the default and only supported database.
 */
export const USE_SUPABASE_DB = true

/**
 * Check if fully migrated to Supabase.
 */
export const IS_SUPABASE_ONLY = true
